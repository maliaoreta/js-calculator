// Loads total's initial value to value that is passed in
module.exports.setTotal = function(num, total){
	var _total = total;
    if(typeof(num)==='number'){
    return _total;
    }
    else{
      throw error;
    }
  };